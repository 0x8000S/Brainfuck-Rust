# 位运算演示
# 展示 ~（按位非）、{（左移/×2）、}（右移/÷2）

`prt(!)     # 数字输出
`double({)  # 翻倍
`half(})    # 折半

5|prt|          # 输出 5
~|prt|          # 按位取反：5(00000101) → 250(11111010)
double|prt|     # 左移：250 × 2 = 500 % 256 = 244
half|prt       # 右移：244 ÷ 2 = 122