# 栈操作演示：后进先出 (LIFO)
# 压入 A、B、C，然后依次弹出

`A(65) `B(66) `C(67)

A|:       # 调用 A（格=65），压栈
B|:       # 调用 B（格=66），压栈
C|:       # 调用 C（格=67），压栈

;|.       # 弹出到当前格（67），输出 'C'
;|.       # 弹出到当前格（66），输出 'B'
;|.       # 弹出到当前格（65），输出 'A'